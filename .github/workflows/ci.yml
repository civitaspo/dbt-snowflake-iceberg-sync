# Single pull_request entrypoint (Shunsuke pattern).
# Required check: status-check (only). Real work lives in wc-*.yml.
# Title edits (`edited`) run title-only; open/sync/reopen/push run the full suite.

name: CI

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize
      - reopened
  push:
    branches:
      - main
  workflow_dispatch:

permissions: {}

concurrency:
  group: >-
    ci-${{ github.workflow }}-${{ github.ref }}-${{
      github.event_name == 'pull_request' && github.event.action == 'edited' && 'title'
      || 'full'
    }}
  cancel-in-progress: true

jobs:
  tests:
    if: github.event_name != 'pull_request' || github.event.action != 'edited'
    uses: ./.github/workflows/wc-tests.yml
    permissions:
      contents: read

  fusion-parse:
    if: github.event_name != 'pull_request' || github.event.action != 'edited'
    uses: ./.github/workflows/wc-fusion-parse.yml
    permissions:
      contents: read

  workflows:
    if: github.event_name != 'pull_request' || github.event.action != 'edited'
    uses: ./.github/workflows/wc-lint.yml
    with:
      autofix: ${{ github.event_name == 'pull_request' }}
    secrets: inherit
    permissions:
      contents: read

  pr-title:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/wc-pr-title.yml
    permissions:
      pull-requests: read

  status-check:
    name: status-check
    if: always() && !cancelled() && github.event_name == 'pull_request'
    needs:
      - tests
      - fusion-parse
      - workflows
      - pr-title
    runs-on: ubuntu-latest
    permissions: {}
    timeout-minutes: 5
    steps:
      - name: Require checks success
        run: |
          set -euo pipefail
          ok() {
            local result="$1"
            local label="$2"
            echo "${label}: ${result}"
            if [[ "${result}" != "success" && "${result}" != "skipped" ]]; then
              exit 1
            fi
          }
          ok "${{ needs.tests.result }}" "tests"
          ok "${{ needs.fusion-parse.result }}" "fusion-parse"
          ok "${{ needs.workflows.result }}" "workflows"
          ok "${{ needs.pr-title.result }}" "pr-title"
