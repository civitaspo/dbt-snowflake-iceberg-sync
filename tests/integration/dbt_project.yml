name: dbt_snowflake_iceberg_sync_integration_tests
version: 0.1.0
config-version: 2

profile: dbt_snowflake_iceberg_sync_integration_tests

model-paths:
  - models

clean-targets:
  - target
  - dbt_packages
